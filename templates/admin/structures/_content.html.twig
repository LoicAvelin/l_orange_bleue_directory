{% for structure in structures %}
<div class="col-12 col-lg-6 col-xl-4 mb-3">
  <div class="card mb-3 h-100 bg-black bg-opacity-50 rounded text-center container" style="min-width: 12rem; max-width: 20rem;">
    <div class="card-header">
      <h2 class="card-title fs-4"><i class="bi bi-house-fill"></i> | <span>{{ structure.name }}</span></h2>
    </div>
    <div class="card-body">
      <p><i class="bi bi-envelope-fill"></i> | {{ structure.address }}</p>
      <p><i class="bi bi-telephone-fill"></i> | {{ structure.phoneNumber }}</p>
      <p><i class="bi bi-calendar-plus-fill"></i> | {{ structure.createdAt | date("d-m-Y") }}</p>
      <p><i class="bi bi-activity"></i> | 
        {% if structure.isActive == 1 %}Oui{% else %}Non
        {% endif %}</p>
      <p></p>
    </div>
    <div class="card-body">
      <div class="mb-3">
        <a onclick="return deleteConfirm()" href="{{ path('delete_structure', {id : structure.id}) }}" class="main__div--button btn btn-outline-light">Supprimer</a>
      </div>
      <div class="mb-3">
        <a href="{{ path('edit_structure', {id : structure.id}) }}" class="main__div--button btn btn-outline-light">Modifier</a>
      </div>
      <div>
        {% if structure.isActive == 1 %}
          <a onclick="return disableConfirm()" href="{{ path('disable_structure', {id : structure.id}) }}" class="main__div--button btn btn-outline-light">Désactiver</a>
        {% else %}
          <a onclick="return enableConfirm()" href="{{ path('enable_structure', {id : structure.id}) }}" class="main__div--button btn btn-outline-light">Activer</a>
        {% endif %}
      </div>
    </div>
  </div>
</div>
{% endfor %}