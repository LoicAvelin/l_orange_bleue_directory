{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Utilisateurs{% endblock %}

{% block stylesheets %} <link rel="stylesheet" href="{{ asset('assets/css/readUsers.css') }}"> {% endblock %}

{% block home %}<a href="/admin">Accueil</a>{% endblock %}
{% block h1 %}Utilisateurs{% endblock %}

{% block main %}
  
    {% for user in users %}
    <div>
      <h2>Nom : {{ user.name }}</h2>
      <h3>Type de compte :
        {% if user.roles == ['ROLE_ADMIN', 'ROLE_MANAGER'] %}Administrateur
        {% elseif user.roles == ['ROLE_PARTNER', 'ROLE_MANAGER'] %}Partenaire
        {% else %}Manager
        {% endif %}
      </h3>
      <h3>Adresse mail : {{ user.email }}</h3>
      <p>Numéro de téléphone : {{ user.phoneNumber }}</p>
      <p>Date de création : {{ user.createdAt | date("d-m-Y") }}</p>
      <p>Actif : 
        {% if user.isActive == 1 %}Oui{% else %}Non
        {% endif %}
      </p>
      <div>
        <a href="{{ path('delete_user', {id : user.id}) }}" class="main__div--button">Supprimer</a>
      </div>
      <div>
        <a href="{{ path('edit_user', {id : user.id}) }}" class="main__div--button">Modifier</a>
      </div>
      <div>
      {% if user.isActive == 1 %}
        <a href="{{ path('disable_user', {id : user.id}) }}" class="main__div--button">Désactiver</a>
      {% else %}
        <a href="{{ path('enable_user', {id : user.id}) }}" class="main__div--button">Activer</a>
      {% endif %}
      </div>
    </div>
    {% endfor %}
  
{% endblock %}