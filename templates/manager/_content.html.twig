{% for user in users %}
<div class="col-12 col-lg-6 col-xl-4 mb-3">
	<div class="card mb-3 h-100 bg-black bg-opacity-50 border-dark text-center container" style="min-width: 12rem; max-width: 20rem;">
		<div class="card-header">
			<h2 class="card-title fs-4"><i class="bi bi-person-fill"></i> |
				<span>{{ user.name }}</span>
			</h2>
		</div>
		<div class="card-body">
			<p><i class="bi bi-person-badge-fill"></i> |
				{% if user.roles == ['ROLE_ADMIN', 'ROLE_MANAGER'] %}Administrateur
					{% elseif user.roles == ['ROLE_PARTNER', 'ROLE_MANAGER'] %}Partenaire
					{% else %}Manager
				{% endif %}
			</p>
			<p><i class="bi bi-envelope-at-fill"></i> |
				{{ user.email }}</p>
			<p><i class="bi bi-telephone-fill"></i> |
				{{ user.phoneNumber }}</p>
			<p><i class="bi bi-calendar-plus-fill"></i> |
				{{ user.createdAt | date("d-m-Y") }}</p>
			<p><i class="bi bi-activity"></i> | 
				{% if user.isActive == 1 %}Oui{% else %}Non
				{% endif %}
			</p>
		</div>
	</div>
</div>
{% endfor %}

{% for structure in structures %}
<div class="col-12 col-lg-6 col-xl-4 mb-3">
	<div class="card mb-3 text-center h-100 bg-black bg-opacity-50 border-dark" style="min-width: 12rem; max-width: 20rem;">
		<div class="card-header">
			<h2 class="card-title fs-4"><i class="bi bi-house-fill"></i> |
				<span>{{ structure.name }}</span>
			</h2>
		</div>
		<div class="card-body">
			<p><i class="bi bi-envelope-fill"></i> |
				{{ structure.address }}</p>
			<p><i class="bi bi-telephone-fill"></i> |
				{{ structure.phoneNumber }}</p>
			<p><i class="bi bi-calendar-plus-fill"></i> |
				{{ structure.createdAt | date("d-m-Y") }}</p>
			<p><i class="bi bi-activity"></i> |
				{% if structure.isActive == 1 %}Oui{% else %}Non
				{% endif %}
			</p>
		</div>
	</div>
</div>
{% endfor %}
